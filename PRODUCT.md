# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- 1차 사용자는 폐쇄망 데스크톱 브라우저에서 프로젝트를 찾고 조건표를 장시간 편집·검토하는 공정 엔지니어다.
- 파라미터 관리자와 검토자·승인자도 사용하지만, 정보 구조와 상호작용의 우선순위는 공정 엔지니어의 작업 흐름에 둔다.

## Product Purpose

PCM은 Process 구조 이해, 대규모 조건표 편집, 잠금·검증·이력 추적을 하나의 끊기지 않는 작업 환경으로 제공한다. 사용자가 구조적 맥락을 잃지 않고 조건을 안전하게 변경하고, 변경 결과와 근거를 신뢰할 수 있게 만드는 것이 목적이다.

## Positioning

PCM은 범용 스프레드시트나 독립된 마스터 데이터 관리 화면이 아니다. Process 구조에서 프로젝트와 조건표를 만들고, 고밀도 편집에서 잠금·검증·이력으로 이어지는 제조 공정 조건 전용 작업 흐름을 제공한다.

## Operating Context

- 폐쇄망의 데스크톱 브라우저와 넓은 모니터에서 사용한다.
- 모바일은 제품 지원 대상이 아니며 모바일 조회·편집·승인 흐름을 설계하지 않는다.
- 공정 엔지니어는 수십~수백 개 프로젝트에서 대상을 찾고, 약 200개 파라미터를 포함하는 조건표를 키보드 중심으로 장시간 편집한다.
- 프로젝트 생성, Process·백본 선택, 조건표 편집, 잠금, 검증, 이력 확인이 주요 업무 흐름이다.

## Capabilities and Constraints

- 프로젝트·공정 카탈로그·파라미터·ChoiceSet·조건표의 기존 기능과 URL 계약을 유지한다.
- Glide Data Grid의 가상화와 고밀도 편집 성능을 유지한다.
- 폐쇄망 제약상 외부 폰트, 런타임 CDN, 신규 네트워크 의존성을 추가하지 않는다.
- React 18, React Router, TanStack Query, Zustand, Tailwind CSS 4, Glide Data Grid의 기존 기술 계약을 보존한다.
- Project, Process, Layer/Step, Parameter, Choice code/label 등 기존 한국어 도메인 용어를 유지한다.

## Brand Commitments

- 제품명은 PCM을 유지한다.
- 사용자 문구는 짧고 직접적이며 존중하는 자연스러운 한국어를 사용한다.
- 문제 안내는 가능한 한 `문제 + 영향 + 다음 행동` 순서로 작성한다.
- 기존 시각 언어는 전면 재설계의 정답이 아니라 제품 맥락을 이해하기 위한 참고 자료로만 사용한다.

## Evidence on Hand

- 실제 구현: `frontend/src/`
- 현재 제품·디자인 기록: `DESIGN.md`
- 기능별 UX 스펙: `docs/superpowers/specs/`
- 화면·업무 흐름 기록: `plan/05-ui-wireframe.md`
- 현재 테스트 계약: `frontend/src/**/*.test.ts` 및 `frontend/src/**/*.test.tsx`
- 실제 조직 로고나 별도 브랜드 자산은 현재 제공되지 않았다. 향후 작업은 이를 임의로 만들거나 외부 브랜드 근거를 발명하지 않는다.

## Product Principles

1. Process 구조에서 편집·검증·이력까지 작업 문맥을 끊지 않는다.
2. 대규모 조건표의 가독성과 키보드 편집 효율을 장식보다 우선한다.
3. 잠금·저장·검증·변경 상태를 명시적으로 보여 신뢰를 만든다.
4. 기존 기능과 URL을 보존하면서 화면의 책임과 내비게이션을 명확히 한다.
5. 오류나 네트워크 실패가 발생해도 검색·입력·선택·dirty 상태를 가능한 한 보존한다.

## Accessibility & Inclusion

- WCAG 2.2 AA를 목표로 한다.
- 모든 주요 업무 동작을 키보드로 수행할 수 있어야 한다.
- 포커스, 선택, 잠금, 저장, 오류, 경고, 성공 상태를 색상에만 의존하지 않고 문구와 구조로 전달한다.
- 긴 데스크톱 작업 세션을 고려해 텍스트 대비, 행간, 밀도, 클릭 영역과 상태 변화를 일관되게 유지한다.
