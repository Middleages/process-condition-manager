DB_NAME=pcm
DB_USER=pcm_user
DB_PASSWORD=pcm_pass
SECRET_KEY=change-this-secret-key

# Frontend API URL
# Leave empty for production (uses relative /api via nginx proxy)
# Set to http://localhost:8000 for local development without nginx
VITE_API_URL=
